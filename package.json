{
  "name": "video-curator",
  "version": "1.0.0",
  "description": "Local web app for triaging a folder of videos: plays them shuffled, Keep leaves the file in place, Reject moves it to a _rejected subfolder.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
